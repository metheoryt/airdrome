# Shared-memory proposals — airdrome, 2026-09-12

Harvested from 5 Claude transcripts (2026-09-08 → 2026-09-09, host g513ie/g15) against
`fcbff68`. Nothing here has been written to any shared store; `/memory-review` applies it.

Format: `tier | action | the fact, as exact text to paste | target file | source | confidence`

## global

global | edit | **`gortex init` also skips an EXISTING project-scope `.mcp.json`.** It prints `skip .mcp.json (already exists)` and does not merge — so a repo that already defines another MCP server (here `postgres`) ends up wired with `.claude/settings.json` and **no gortex server entry at all**, and the tools never mount. Merge the entry in by hand (gortex first, existing servers preserved) and print the file back to confirm. This is a separate case from the user-scope skip below. | ~/.claude/memory/global.md, `## Gortex` — amends the bullet beginning "**`gortex init` writes NO `.mcp.json` when a user-scope server already exists**" | session e30626c8-b48e-41aa-88bf-1c644e6ba74e | high

global | add | **Gortex's PreToolUse hook blocks a *Bash* command whose text matches indexed symbols.** A plain `grep -n -i "music" ~/.gitignore` was refused because "music" matched 10 indexed symbols — the hook reads the command string, not the path being read, so a shell command touching a file outside any tracked repo is still refused. Rewrite the literal out of the command (`awk '/Music/ {print NR": "$0}'`) rather than fighting the hook. | ~/.claude/memory/global.md, `## Gortex` | session 0ff09805-610a-4caf-8a2a-377c7283e204 | high

global | add | **The Gortex CLI has no bare verbs — everything is `gortex call <tool> --arg k=v`.** `gortex usages --symbol X` is not a command; the form is `gortex call relations --arg operation=usages --arg 'target={"symbol":"<id>"}' --arg 'output={"format":"gcx"}'`. In a multi-repo daemon a symbol or path must carry its repo prefix (`airdrome/AGENTS.md`, `airdrome/airdrome/match.py::build_match_score`); an unprefixed one errors with "path names a file in multiple tracked repos; prefix it with one of: …". | ~/.claude/memory/global.md, `## Gortex` | sessions e30626c8, 0ff09805 | high

global | add | **An MCP server launched through `uvx` that does not cap its `mcp` SDK dependency breaks on the 2.x SDK.** FastMCP was renamed MCPServer there, so a v1-era package dies at import with `ModuleNotFoundError: No module named 'mcp.server.fastmcp'` and exits before the handshake; the client only ever shows a failed reconnect / `CONNECTION_CLOSED`, which reads as a fault in the thing the server talks to. Pin it in the server's args: `"args": ["--python","3.12","--with","mcp<2","postgres-mcp", …]`. Verify by running the same command by hand with the env var set and reading stderr — the MCP client shows none of it. | ~/.claude/memory/global.md, `## Gortex` or a tooling section | session 862242fe-e363-45b9-b556-578e9866fa0f | high

global | add | **MusicBrainz Picard 3: its config is a QSettings INI that Picard rewrites WHOLE on exit**, so any external edit must be made with Picard closed and then verified across a real launch/quit cycle (md5 the ini before and after). Write it through Picard's own config API (`sys.path.insert(0,'/usr/lib/picard')`; `import picard.options`; `picard.config.setup_config(app, ini)`) because structured values such as `list_of_scripts` are pickled QVariant blobs, not plain ini text. Option names changed in 3: `standardize_artists` → `standardize_artist_names` (now a `StandardizeArtistNames` Enum), `image_filename` → `cover_image_filename`. `picard-cli` supports only `completions`, `plugins`, `profiles` — there is no headless tagging. | ~/.claude/memory/global.md, new `## MusicBrainz Picard` | session 0ff09805 | high

global | add | **Picard needs no AcoustID key to Scan.** It ships its own *application* key (`ACOUSTID_KEY = 'v8pQ6oyB'` in `picard/const/__init__.py`) and uses it for every lookup; the `acoustid_apikey` setting is the user's *account* key and is passed as `user=` to `/submit` only, i.e. it enables contributing fingerprints back. Submissions are public and permanent, so submit only from matches already confirmed. (Verified end to end: an untagged file resolved at score 0.985 with no user key set.) | ~/.claude/memory/global.md, `## MusicBrainz Picard` | session 0ff09805 | high

global | add | **mutagen returns MP4 freeform atom values as `bytes`.** `str()` on one yields `"b'<uuid>'"`, so the identical MusicBrainz album ID compares unequal between an `.m4a` and the `.mp3` beside it — which fakes "this file changed" findings on any mixed-format folder (it faked 26 of 50 in the case measured). Decode explicitly (`value.decode('utf-8','replace')`) on both sides, and remember a JSON snapshot taken with the naive `str()` needs the same unwrapping on read-back. | ~/.claude/memory/global.md, a tagging/audio section | session 0ff09805 | high

global | add | **Navidrome tag mapping, two limits that only bite MP4** (read from `navidrome/resources/mappings.yaml`, not inferred): there is **no MP4 alias for `ALBUMARTISTS`**, and on MP4 Picard's `date` lands in `©day`, which Navidrome reads as *releasedate*. So of Navidrome's three documented Picard tagger scripts, the multi-value `ARTISTS` one works on m4a while the `ALBUMARTISTS` and release/original-date ones are partly inert there; MP3 is unaffected. Also: Picard 3 writes `TXXX:ALBUMARTISTS` natively, so `$setmulti(albumartists,…)` only adds a duplicate lowercase frame, and `$if(%_recordingcomment%,$set(subtitle,…))` fills `TIT3` with bare words like "explicit" that Navidrome renders as a track subtitle. | ~/.claude/memory/global.md, `## MusicBrainz Picard` | session 0ff09805 | high

global | add | **A failing AcoustID (or any `urllib`) POST raises a bare `HTTPError: HTTP Error 400` with no reason.** The JSON body carrying the actual cause (`{"error":{"code":4,"message":"invalid API key"}}`) is only in `e.read()`. Catch `urllib.error.HTTPError` and print the body before concluding anything about the request. | ~/.claude/memory/global.md, `## Git & bash footguns` or a scripting section | session 0ff09805 | medium

global | add | **`wsl.exe -l -q` through a non-interactive ssh into a Windows host returns nothing and can hang**, while `wsl -l -v` returns UTF-16LE that needs `[Console]::OutputEncoding=[System.Text.Encoding]::UTF8` on the remote side plus `tr -d '\000'` locally to be readable at all. Enumerating a Windows box's distros over ssh is therefore not a one-liner; budget a real timeout and expect empty output to mean "wrong invocation", not "no distros". | ~/.claude/memory/global.md, `## Windows & WSL scripting footguns` | session f284b5fb-12d6-495c-802e-dee1b30a9419 | medium

## host:g15

host:g15 | add | **Cover art never downloads in Picard on this box, and the cause is the ISP SNI filter, not Picard.** `coverartarchive.org` answers 200 but 307s the actual image to `archive.org`, whose HTTPS is dropped here while plain HTTP returns 200 — the exact signature recorded under `## ISP-level TLS-SNI filtering in KZ` in global memory. Result: the `save_images_to_tags` / `save_images_to_files` settings are inert, existing embedded artwork (iTunes-era PNGs) is left as-is and no `cover.jpg` is written. Symptom to recognise: one album folder whose tracks carry two different images. Run Picard behind the local `ciadpi` SOCKS proxy for a session where artwork matters. | ~/.claude/host-memory.md | session 0ff09805 | high

host:g15 | add | **Two music trees, two roles.** `~/Music/PicardedMusic` (66 G, 6,986 audio files — 5,775 mp3 / 1,211 m4a) is the Picard-tagged source of truth, copied from the iTunes library and never written by airdrome; `~/Music/Airdrome` is airdrome's `LIBRARY_DIR` — its managed output (`Library/` + `Copies/`, ~64 G copied, plus the `.airdrome/duplicates.json` mirror and the cloud exports). `~/Music/OldMusic` is a third, untouched pile. Do not point `LIBRARY_DIR` at PicardedMusic: `organize` would write a second layout inside the tree it just imported, and the next `import` would re-ingest airdrome's own output. | ~/.claude/host-memory.md | sessions f284b5fb, 0ff09805 | high

host:g15 | add | **The Picard tagging helpers and the pre-tagging tag baseline are dotfiles-tracked on branch `g15`**: `Music/tagging-tools/{README.md,apply-picard-settings.py,audit.py,worklist.py}` and `Music/picard-baseline-*.json` (the allow-line is a glob, so a future snapshot needs no `.gitignore` edit). `worklist.txt` is deliberately untracked — it regenerates from `worklist.py`. The baseline is the one irreplaceable file: it is the snapshot of every file's tags before mass retagging, and it cannot be regenerated once Picard has overwritten them. | ~/.claude/host-memory.md | session 0ff09805 | high
